# CALCULA FIBONACCI

7 # n-esimo número da sequênca

0 # Contador

1 # Valor de incremento

# Variaveis auxiliares a, b e c
0
1
0

0 # Resultado

.TEXT
# Escreve resposta (valor de a)
LOAD M(3)
STOR M(6)

# Se contador >= posição do número desejado, encerra execução
LOAD M(1)
SUB M(0)
SUB M(2)
JUMP+ M(999,0:19)

# Faz a avançar na sequência
LOAD M(3) # carrega a
STOR M(5) # salva a em c
ADD M(4)  # soma a com b
STOR M(3) # salva a
LOAD M(5) # carrega c
STOR M(4) # salva c em b

# Incrementa contador
LOAD M(1)
ADD M(2)
STOR M(1)

# Volta pro inicio
JUMP M(100,0:19)
